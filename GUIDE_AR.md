# دليل إنشاء نظام وكالة الناصر للملاحة البحرية (خطوة بخطوة)

هذا الدليل يشرح بالتفصيل الممل كيفية بناء هذا النظام المتكامل لإدارة العمليات البحرية والجمركية.

---

## الخطوة 1: التخطيط واختيار التقنيات (Tech Stack)

تم اختيار الأدوات التالية لضمان السرعة، الأداء، وسهولة النشر:
- **البيئة الأساسية**: Node.js مع TypeScript.
- **الإطار الأمامي (Front-end)**: React 19 مع Vite لسرعة التطوير.
- **الإطار الخلفي (Back-end)**: Express.js (Full-stack Mode).
- **قاعدة البيانات**: SQLite (باستخدام مكتبة `better-sqlite3`) لسهولة الأرشفة والسرعة في المعالجة المحلية.
- **التصميم**: Tailwind CSS للواجهات العصرية و Lucide-React للأيقونات.
- **التحريك**: Framer Motion (Motion) لإضافة لمسات تفاعلية.

### إعداد بيئة التطوير الأساسية:
1. إنشاء مشروع Vite جديد باستخدام `npm create vite@latest`.
2. تنصيب المكتبات البرمجية الضرورية:
   - `npm install express better-sqlite3 cors multer xlsx` (للبكيند).
   - `npm install lucide-react framer-motion react-router-dom` (للفرونتند).
   - `npm install -D @types/express @types/better-sqlite3 tsx` (أدوات التطوير).

---

## الخطوة 2: هيكلة قاعدة البيانات (Database Schema)

النظام مبني على 14 جدولاً مترابطاً لضمان تغطية كافة جوانب العمل:

1.  **users**: لإدارة الموظفين، كلمات المرور، والصلاحيات (Admin, Customs, Shipping).
2.  **ships**: تسجيل بيانات البواخر، مواعيد الوصول والمغادرة، ووكلاء الشحن.
3.  **bills**: البوالص المرتبطة بالبواخر، مع أوزانها وأنواع بضائعها.
4.  **headings**: البنود الجمركية وقيم الرسوم الخاصة بكل بند.
5.  **customs_data**: البيانات الجمركية (استيراد/تصدير/ترانزيت) وروابطها بالبوالص والبواخر.
6.  **deposits (IM77)**: إدارة بيانات الإيداع في المنطقة الحرة.
7.  **invoices**: فواتير المنطقة الحرة (محلية/ترانزيت) وربطها ببيانات الإيداع.
8.  **exits**: بيانات إخراج (سحب) البضائع من المنطقة الحرة.
9.  **cars**: سجل الشاحنات والسائقين لتنظيم عمليات التحميل.
10. **operations**: سجل حي لعمليات التفريغ والتحميل (من قبل أي موظف، وفي أي وقت وأي سيارة).
11. **deleted_operations**: سجل للمحذوفات مع ميزة "الاستعادة" (Audit Log).
12. **settings**: بيانات الوكالة (الاسم، الشعار، أرقام التواصل).
13. **app_instructions**: نظام توجيهات من المدير للموظفين مع خاصية الرد.
14. **notifications**: تنبيهات النظام لعمليات الدخول والخروج.

---

## الخطوة 3: إعداد الخادم (Server-side)

تم إنشاء ملف `server.ts` ليقوم بالوظائف التالية:
1.  **بدء التشغيل**: تهيئة قاعدة البيانات وإنشاء الجداول تلقائياً إذا لم تكن موجودة.
2.  **المهاجرة (Migrations)**: التحقق من وجود الأعمدة وتحديثها لضمان عدم ضياع البيانات عند التحديث.
3.  **نظام المصادقة**: مسار `/api/login` للتحقق من المستخدم وتعيين حالته "متصل".
4.  **CRUD API**: بناء مسارات (GET, POST, PUT, DELETE) لكل الجداول المذكورة أعلاه.
5.  **نظام النسخ الاحتياطي**: مسار `/api/restore` الذي يستقبل بيانات Excel ويقوم بإعادة كتابتها في الجداول المناسبة بدقة عالية.

---

## الخطوة 4: إعداد الواجهة الأمامية (Client-side)

1.  **نظام التوجيه (React Router)**: تقسيم التطبيق إلى صفحات (Login, Dashboard, Ships, Bills, etc).
2.  **سياق المصادقة (AuthContext)**: إدارة حالة المستخدم الحالي، صلاحياته، ووظيفة تسجيل الخروج عبر كامل التطبيق.
3.  **المكونات الأساسية (Components)**:
    - `DataTable`: مكون ذكي لعرض البيانات، البحث، الترقيم (Pagination)، والفرز.
    - `Layout`: الشريط الجانبي (Sidebar) الذي يتغير بناءً على صلاحيات المستخدم.
    - `UnitSelector`: مكون موحد لاختيار وحدات التعبئة مع إمكانية إضافة وحدات جديدة فوراً.
    - `ErrorBoundary`: واجهة حماية في حال حدوث أي خطأ برمجي غير متوقع.

---

## الخطوة 5: بناء الصفحات بالتفصيل

1.  **لوحة التحكم (Dashboard)**: تعرض إحصائيات حية (أوزان اليوم، السيارات، الموظفين النشطين) مع تنبيهات فورية للتعليمات الجديدة.
2.  **المنطقة الحرة (Free Zone)**: صفحة معقدة تحتوي على 3 تبويبات (إيداع، فواتير، إخراج) مع ميزة ربط البيانات التلقائي (Auto-fill) عند اختيار رقم بيان IM77.
3.  **العمليات (Operations)**: واجهة سريعة لموظفي الشحن لتسجيل حمولة كل سيارة مرتبطة ببولصتها.
4.  **التقارير (Reports)**:
    - تقارير مفصلة لكل قسم.
    - ميزة "مقارنة الأرصدة" في المنطقة الحرة لمعرفة المتبقي من كل بيان إيداع (الوزن الأصلي ناقص الأوزان المسحوبة).
    - تصدير التقارير إلى Excel مع اختيار الأعمدة المطلوبة.

---

## الخطوة 6: ميزات الأمن والخصائص الإضافية

- **نظام الصلاحيات**: المدير فقط يرى كل شيء (سجل المحذوفات، إعدادات النظام، الموظفين). موظف الجمارك يميل للبيانات الجمركية، وموظف الشحن للعمليات.
- **سجل المحذوفات**: عند حذف أي عملية شحن، لا يتم مسحها نهائياً بل نُقلت إلى `deleted_operations` مع ذكر اسم الشخص الذي حذفها، لتمكين المدير من استعادتها.
- **النسخ الاحتياطي الذكي**: في صفحة الإعدادات، زر واحد يقوم بجمع كل بيانات قاعدة البيانات وتحويلها لملف Excel بتبويبات متعددة، وزر آخر لاستعادتها بالكامل.

---

## الخطوة 7: الخطوات النهائية للنشر

1.  تنفيذ `npm install` لتثبيت المكتبات.
2.  تنفيذ `npm run build` لإنتاج نسخة الإنتاج.
3.  تشغيل الخادم `tsx server.ts` الذي سيتكفل بخدمة ملفات الموقع والواجهة البرمجية معاً.

---
**تم إعداد هذا النظام ليكون قابلاً للتوسع والاستخدام المهني الشاق.**
